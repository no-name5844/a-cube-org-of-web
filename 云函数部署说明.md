# 云函数部署说明（Supabase Edge Functions）

安全敏感的后端操作已从前端直连数据库迁到 **Supabase Edge Functions**（Deno），**强制走函数、无降级路径**——函数未部署或不可用时对应操作直接报错。

## 三个云函数

| 函数 | 作用 | 服务端强制的规则 |
|------|------|------------------|
| `submit-attempt` | 成绩提交 | 编辑员及以上才可提交；`status` 由服务端按数据库角色决定（admin→approved 直接生效，editor/reviewer→pending 待审核）；`submitted_by` 取服务端解析的身份；字段白名单过滤，前端多余字段（如伪造 status）一律丢弃 |
| `review-attempt` | 审核成绩 | 审核员及以上才可审核；`reviewed_by` 一律取服务端身份，无法伪造审核人 |
| `assign-role` | 用户角色管理 | 管理员才可操作；只能授予 user/editor/reviewer；不能授予 admin；不能修改其他管理员；不能修改自己 |

所有函数的共同安全模型：

1. **身份验证**：Supabase 网关先做 `verify_jwt`（无有效登录 JWT 的请求直接被网关拒绝），函数内再用 JWT 换取用户身份；
2. **角色完全信任数据库**：每次调用都从 `profiles` 现读角色，前端缓存不作数；
3. **SERVICE_ROLE 密钥只存在于函数环境**：仅在服务端自校验通过后用于绕过 RLS 完成受控写入，绝不下发给前端。

## 部署步骤

```bash
# 1. 安装 Supabase CLI（如未安装）
npm install -g supabase

# 2. 登录并关联项目
supabase login
supabase link --project-ref <你的项目ref>

# 3. 部署三个函数（在仓库根目录执行）
supabase functions deploy submit-attempt
supabase functions deploy review-attempt
supabase functions deploy assign-role
```

- `SUPABASE_URL` 与 `SUPABASE_SERVICE_ROLE_KEY` 由 Supabase 运行时自动注入，无需手动配置。
- 前端无需任何配置——`dbClient.functions.invoke(...)` 自动使用已连接项目的 URL，并自动携带当前登录用户的 JWT。

## 前端对应改动

- `js/data-saver.js` `addAttempt()` → `functions.invoke('submit-attempt')`
- `js/admin.js` `reviewAttempt()` → `functions.invoke('review-attempt')`
- `js/admin.js` `changeUserRole()` → `functions.invoke('assign-role')`

三处均**不再直写数据库**。数据库 RLS 策略（schema-v6-auth.sql）继续保留，作为第二道防线。

## 注意事项

- 未登录用户调用会被网关 401 拒绝——普通用户提交成绩会得到「需要编辑员及以上权限」，符合权限矩阵。
- 部署后建议在 Supabase Dashboard → Edge Functions 页面确认三个函数状态为 Active。
- 函数日志：Dashboard → Edge Functions → 对应函数 → Logs，排查线上问题时看这里。
